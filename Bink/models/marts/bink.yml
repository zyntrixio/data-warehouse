version: 2

models:
    - name: user
      description: "User table containing current and historical users"
      schema: 'Bink'
      columns:
        - name: 'ID'
          description: Primary Key of the table
          quote: true
          tests:
            - unique
            - not_null
    - name: transactions
      description: "User table containing historical transactions"
      schema: 'Bink'
      columns:
        - name: 'TRANSACTION_ID'
          description: Primary Key of the table
          quote: true
          tests:
            - unique
            - not_null
        - name: 'STATUS'
          description: Transaction most recent status
          quote: true
          tests:
            - unique
            - not_null
            - accepted_values:
                values: ['placed', 'shipped', 'completed', 'returned']
